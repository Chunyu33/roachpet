import type { Roach } from '../types/roach'; import './RoachView.css';
export function RoachView({roach,onClick}:{roach:Roach;onClick:()=>void}){return <button className={`roach ${roach.state.toLowerCase()}`} style={{left:roach.position.x,top:roach.position.y,transform:`scaleX(${roach.direction})`}} onClick={onClick} aria-label="蟑螂"><span className="antenna a1"/><span className="antenna a2"/><span className="body"/><span className="wing wing1"/><span className="wing wing2"/><span className="leg l1"/><span className="leg l2"/><span className="leg l3"/><span className="leg l4"/></button>}
